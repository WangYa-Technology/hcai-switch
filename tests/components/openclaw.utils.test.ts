import { describe, it } from "vitest";

// OpenClaw UI utilities were removed with the managed OpenClaw app.
describe("openclaw utils (removed)", () => {
  it("placeholder keeps suite green after OpenClaw removal", () => {
    // intentionally empty
  });
});
